# values.yaml
# This file contains default values for the Helm chart.
# Replace these placeholder values with actual values when deploying.

robusta:
  slack_api_key: ""  # Add your Slack API key here (e.g., xoxb-xxxx-xxxx-xxxx)
  slack_channel_name: ""  # Specify the Slack channel name for notifications (e.g., my-channel)
  cluster_name: ""  # Name of the Kubernetes cluster (e.g., production-cluster)