apiVersion: v1
kind: Service
metadata:
  name: my-react-app
spec:
  type: LoadBalancer
  selector:
    app: my-react-app
  ports:
  - name: "{{ .Values.dashboard_service_ports.name }}"
    port: 80
    targetPort: 3000
